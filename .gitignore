.vscode
build
install
log
__pycache__
.env
# ricar-launch/.env трекаем (IMAGE/CMD + тумблеры LATERAL/TRAJECTORY), нужен стеку
!ricar-launch/.env
core.*

# upstream lio_sam demo-ассеты (gif/pdf, ~64МБ) — роботу не нужны
workspace/src/lio_sam/config/doc/
# saved point clouds (lio_sam savePCD / тестовые карты) — не нужны в репо
*.pcd
