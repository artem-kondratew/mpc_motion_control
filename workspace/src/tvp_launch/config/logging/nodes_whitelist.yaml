# СПРАВОЧНО. Имена нод под namespace робота /<vehicle_id>/...  (пример: vehicle_id=alpha).
# Проверить фактические: ros2 node list
nodes:
  # контроллеры (снапшот тюнинга — самое важное)
  - /alpha/control/swarm_cc_mpc_node
  - /alpha/control/swarm_lat_mpc_node
  - /alpha/control/lane_publisher

  # шасси
  - /alpha/kobuki_ros_node
  - /alpha/cmd_vel_wrapper

  # локализация (lio_sam)
  - /alpha/lio_sam_imuPreintegration
  - /alpha/lio_sam_imageProjection
  - /alpha/lio_sam_featureExtraction
  - /alpha/lio_sam_mapOptimization

  # сенсорика (livox, под namespace sensing)
  - /alpha/sensing/livox_mid360_publisher
  - /alpha/sensing/rewrite_mid360_frames

  # transforms
  - /alpha/robot_state_publisher

  # tools (если поднят)
  - /alpha/localization/twist_estimator_node
